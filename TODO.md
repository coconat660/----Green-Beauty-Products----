# TODO - Add to Cart (Functional)

## Plan Steps

1. Create cart state management (context + reducer) with localStorage persistence.
2. Add Cart UI component + `/cart` route (quantity update, remove, total, checkout button).
3. Add Checkout component + `/checkout` route that forwards cart total to payment.
4. Update Getproduct to include an “Add to cart” button per product.
5. Update Mpesapayment to support cart/checkout flow (send correct amount).
6. Update App.js navigation to include Cart.
7. Run build/tests to ensure app compiles.
